{
  "name": "node-url-shortening-service",
  "version": "0.0.1",
  "description": "URL Shortening service written in NodeJS and Express",
  "engines": {
    "node": "5.9.1"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src -d lib",
    "start": "npm install && npm run build && node lib/index.js"
  },
  "dependencies": {
    "pg": "4.x",
    "express": "4.13.3",
    "lodash": "^3.10.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/obstacle-coder/node-url-shortening-service.git"
  },
  "keywords": [
    "node",
    "heroku",
    "express"
  ],
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0"
  }
}
